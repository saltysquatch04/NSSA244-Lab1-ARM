# NSSA244-Lab1-ARM

this template deploys a resource group that contains a virtual network that an Ubuntu VM is a part of. That Ubuntu VM receives a public IP address and there is a network security group created as well that allows inbound traffic on specific ports
